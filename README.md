# Python-Webscraper
A simple webscraper written in python using the Requests-HTML library.

## Feature to do list:
- Run a main loop so user can enter multiple websites
- Add formats which the data can be saved in (text, csv)
- Add a way for users to select what should be scraped